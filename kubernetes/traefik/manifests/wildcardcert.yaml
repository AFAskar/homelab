apiVersion: cert-manager.io/v1
kind: Certificate
metadata:
  name: home-internal-wildcard
  namespace: kube-system
spec:
  secretName: home-internal-wildcard-tls
  duration: 2160h
  renewBefore: 360h
  commonName: "*.home.internal"
  subject:
    organizations:
      - Homelab
  issuerRef:
    name: homelab-ca
    kind: ClusterIssuer
  usages:
    - digital signature
    - server auth
  dnsNames:
    - "*.home.internal"
    - home.internal

  privateKey:
    algorithm: ECDSA
    size: 256
    rotationPolicy: Always
