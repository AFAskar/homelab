apiVersion: v1
kind: Service
metadata:
  name: adguard-ui
spec:
  ports:
    - name: http
      protocol: TCP
      port: 3000
      targetPort: 3000
---
apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: adguard-ui
spec:
  ingressClassName: traefik
  rules:
    - host: dns.home.arpa
      http:
        paths:
          - path: /
            pathType: Prefix
            backend:
              service:
                name: adguard-ui
                port:
                  name: http
